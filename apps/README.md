# Apps

This directory contains product surfaces.

Current contents:

- `macos/PromptForge/`: the SwiftUI macOS app

Read this as the user-facing layer of the repo. The app talks to the local engine through the helper process; it is not where evaluation logic lives.
